---
title: "Enviar imagem"
description: "Envie mensagens com imagens via WhatsApp"
api: "POST https://zapi.hubmessage.io/channels/{channel_id}/token/{channel_token}/send-image"
---

import { projectName } from '/snippets/variables.mdx';

## Conceituação

Método para envio de imagens. Você pode enviar a imagem de duas formas: por **URL** ou por **Base64**.

---

## Atributos

### Header

| Atributo | Tipo | Obrigatório | Descrição |
|---|---|---|---|
| `Authorization` | string | Sim | `Bearer {seu_secret_key}` |
| `Content-Type` | string | Sim | `application/json` |

### Body

| Atributo | Tipo | Obrigatório | Descrição |
|---|---|---|---|
| `phone` | string | Sim | Telefone do destinatário no formato DDI + DDD + número. Ex: `5511999999999` |
| `image` | string | Sim | URL da imagem ou dados em Base64 |
| `caption` | string | Não | Legenda da imagem |
| `messageId` | string | Não | ID de uma mensagem existente para responder |
| `delayMessage` | number | Não | Delay em segundos antes do envio (1-15). Padrão: 1-3 segundos |
| `viewOnce` | boolean | Não | Enviar como mensagem de visualização única |

---

## Exemplo de requisição

```bash
curl -X POST https://zapi.hubmessage.io/channels/SEU_CANAL/token/SEU_TOKEN/send-image \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer SEU_SECRET_KEY" \
  -d '{
    "phone": "5511999999999",
    "image": "https://exemplo.com/imagem.png",
    "caption": "Confira essa imagem!"
  }'
```

### Body com URL

```json
{
  "phone": "5511999999999",
  "image": "https://exemplo.com/imagem.png",
  "caption": "Confira essa imagem!",
  "viewOnce": false
}
```

### Body com Base64

```json
{
  "phone": "5511999999999",
  "image": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAyAAAAMgCAIAAABUEpE...",
  "caption": "Confira essa imagem!",
  "viewOnce": false
}
```

<Warning>
  Ao enviar em Base64, é obrigatório incluir o prefixo com o tipo da imagem, por exemplo: `data:image/png;base64,`. Para validar, cole o Base64 completo na barra de endereço do navegador e verifique se a imagem renderiza corretamente.
</Warning>

<Note>
  Os limites de tamanho e formatos suportados seguem as políticas do WhatsApp. Consulte a [documentação oficial do WhatsApp](https://developers.facebook.com/docs/whatsapp/api/media) para mais detalhes.
</Note>

---

## Resposta

### 200 - Sucesso

```json
{
  "zaapId": "3999984263738042930CD6ECDE9VDWSA",
  "messageId": "D241XXXX732339502B68",
  "id": "D241XXXX732339502B68"
}
```

| Campo | Tipo | Descrição |
|---|---|---|
| `zaapId` | string | Identificador interno |
| `messageId` | string | ID da mensagem no WhatsApp |
| `id` | string | Mesmo valor do `messageId` (compatibilidade) |

<Note>
  A estrutura de resposta é idêntica à da Z-API. Seu código existente que processa esses retornos continuará funcionando sem alterações.
</Note>

### 405 - Método não permitido

Verifique se o método HTTP utilizado é `POST`.

### 415 - Content-Type inválido

Verifique se o header `Content-Type: application/json` está sendo enviado corretamente.
