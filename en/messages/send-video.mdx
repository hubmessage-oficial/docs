---
title: "Send video"
description: "Send video messages"
openapi: "/en/messages/openapi-video.json POST /v1/channels/{channelId}/messages"
---

## Overview

Sends a video message to a contact. The `content.type` field must be `VIDEO` and the video is sent via URL in the `attachments` array.

Optionally, you can include a `caption` with the video.

<Note>
  This endpoint only works when the [conversation window](/en/conversation-window) is open (24 hours).
</Note>

Accepted formats: MP4, 3GP, MKV, AVI, MOV (converted to MP4 H.264/AAC; 3GP is passed through); max size **16 MB**; caption up to 1024 characters. See all the details at [Media validations](/en/messages/media-validations).
