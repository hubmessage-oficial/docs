---
title: "Send video"
description: "Send video messages"
openapi: "/messages/openapi-video.json POST /v1/channels/{channelId}/messages"
---

## Overview

Sends a video message to a contact. The `type` field must be `VIDEO` and the video is sent via URL in the `attachments` array.

Optionally, you can include a `caption` with the video.

<Note>
  This endpoint only works when the [conversation window](/en/conversation-window) is open (24 hours).
</Note>
