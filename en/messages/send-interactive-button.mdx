---
title: "Send text with buttons"
description: "Send text messages with quick reply buttons"
openapi: "/messages/openapi-interactive-button.json POST /v1/channels/{channelId}/messages"
---

## Overview

Sends a text message with quick reply buttons. The `type` field must be `INTERACTIVE_BUTTON`. The recipient can choose from the presented options.

Optionally, you can include an **image** or **video** with the message using the `thumbnail` and `mimeType` fields inside `body`.

<Note>
  This endpoint only works when the [conversation window](/en/conversation-window) is open (24 hours).
</Note>
