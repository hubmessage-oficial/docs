---
title: "Send text"
description: "Send simple text messages"
openapi: "/messages/openapi.json POST /v1/channels/{channelId}/messages"
---

## Overview

Sends a simple text message to a contact. The `type` field must be `TEXT`.

<Note>
  This endpoint only works when the [conversation window](/en/conversation-window) is open (24 hours).
</Note>
