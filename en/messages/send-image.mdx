---
title: "Send image"
description: "Send image messages"
openapi: "/en/messages/openapi-image.json POST /v1/channels/{channelId}/messages"
---

## Overview

Sends an image message to a contact. The `content.type` field must be `IMAGE` and the image is sent via URL in the `attachments` array.

Optionally, you can include a `caption` with the image.

<Note>
  This endpoint only works when the [conversation window](/en/conversation-window) is open (24 hours).
</Note>

Accepted formats: JPEG, PNG, BMP, TIFF, GIF, WebP (non JPEG/PNG are converted to JPEG); max size **5 MB**; caption up to 1024 characters. See all the details at [Media validations](/en/messages/media-validations).
