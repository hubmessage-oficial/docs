| Channel | Support | Notes |
| --- | --- | --- |
| **WhatsApp Business** | ● Supported | Only **one** URL button — extra buttons are discarded. `header` and `footer` are native fields. |
| **Instagram** | ○ Coming soon | The platform accepts URL buttons, but sending is not available yet. |
| **Messenger** | ○ Coming soon | Buttons currently arrive as quick replies and **do not open the link**. `header` and `footer` are discarded. |
| **Telegram Bot** | ● Supported | URL buttons in an inline keyboard, one per row. `header` and `footer` are appended to the text. |
